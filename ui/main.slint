import { AppWindow, Router } from "app_window.slint";
import { PluginItem, PluginItems } from "pages/plugins.slint";
import { Settings } from "pages/settings.slint";
import { PishockSettings } from "plugins/pishock.slint";

export {
    AppWindow,
    Router,
    PluginItem,
    PluginItems,
    Settings,
    PishockSettings,
}
